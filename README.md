# Кодик — итерация после user testing

Учебное приложение на React, TypeScript и Blockly: 22 упражнения в 5 главах, от первой команды до самостоятельных строк Python. Существующий IR-движок сохранён.

## Запуск

```powershell
npm.cmd install
npm.cmd run local
```

Production-сборка: **http://127.0.0.1:4173/**. Скрипт собирает приложение и запускает скрытый фоновый preview только на этом компьютере. Логи: `local-server.log`, `local-server-error.log`; PID: `local-server.pid`. Занятый порт не освобождается автоматически: можно выбрать `npm.cmd run local -- 4174`.

Разработка: `npm.cmd run dev`. После изменений выполните `npm.cmd run build`: уже работающий preview подхватит новую сборку. Прогресс привязан к браузеру и адресу.

## Обучение

Первое упражнение начинается с пустого поля: добавить команду печати, отдельно добавить текст, изменить значение, увидеть Python и проверить результат. Каждая новая механика получает пошаговое знакомство. Уже освоенные знакомства автоматически не повторяются. Библиотека открывается постепенно; текущий шаг выделяет нужную кнопку или поле.

Python постоянно присутствует под блоками, обновляется при изменениях и подсвечивает связь с выбранным типом блока. На маленьком экране к нему можно прокрутить; после завершения шагов знакомства он показывается автоматически. Есть распознавание строки, заполнение пропуска, сборка строки из частей и самостоятельный ввод одной/двух команд печати.

10 знакомств проходят без оценки. 12 самостоятельных упражнений дают до 36 звёзд:

- 3 звезды: первая успешная проверка без подсказок.
- 2 звезды: вторая/третья проверка либо одна содержательная подсказка.
- 1 звезда: четвёртая и последующие проверки, две подсказки либо готовое решение.

Редактирование, очистка, отмена и справка по интерфейсу бесплатны. Повторное открытие того же примера по блокам считается одной подсказкой за прохождение. Повтор запускает новую попытку, сохраняя лучший результат. Следующая глава требует завершения всех заданий предыдущих глав и примерно двух третей звёзд: 6/9, затем 4/6, 4/6 и 4/6. В каждой главе есть итоговая практика.

## Сохранение и аналитика

Локально сохраняются завершение, лучшие звёзды, попытки и подсказки, изученные понятия, текущая глава/урок, шаг знакомства, черновики и ответы. Формат версии 2 использует прежний ключ `kodik-progress-v1`. При миграции старые завершения сохраняются; самостоятельные пройденные упражнения получают 2 звезды, изменённые стартовые черновики 1/2/6/7 сбрасываются. При отказе записи обучение продолжается в памяти вкладки с предупреждением.

Для повторного user testing собираются последние 500 локальных событий: открытие задания, первое действие, добавление блока, шаг знакомства, проверка, подсказка, раскрытие Python, повтор. Введённый текст в события не записывается. Внизу карты курса есть выгрузка JSON. Никакой внешней аналитики нет.

## Архитектура

- `src/course.ts`, `courseBase.ts`: главы и задания, совместимые ID старого курса.
- `src/tutorial.ts`: повторно используемые описания шагов и условия их завершения.
- `src/BlocklyEditor.tsx`: Blockly, соединение блоков, редактирование, фокус и черновики.
- `src/learningEngine.ts`: Blockly → IR → Python, ограниченный интерпретатор и проверка результата/структуры.
- `src/textLearning.ts`: безопасное преобразование учебного текстового ответа в тот же IR.
- `src/achievement.ts`: оценка, сессии, доступность глав.
- `src/LearningLesson.tsx`, `LearningUI.tsx`, `App.tsx`: занятие, диалоги, главная и карта.
- `src/progress.ts`, `analytics.ts`: сохранение, необязательный Supabase, локальные события.

Произвольный Python не исполняется. Поддерживаются IR-команды печати, числа, переменные, арифметика, сравнения, условия, повторения и простые функции. В текстовых заданиях разрешены только строки `print` с текстовым литералом. Нет eval, импортов и доступа учебной программы к сети/файлам/ОС. Действуют лимиты выполнения: 1000 шагов, 40 вложенных вызовов, 200 блоков, строки до 2000 символов, абсолютные значения чисел до 10¹².

Blockly загружается отдельно при открытии занятия. Внешних шрифтов и ресурсов учебного интерфейса нет. Загруженное занятие работает при потере сети; загрузка с нуля без сети и service worker не реализованы.

## Supabase (необязательно)

1. Применить `supabase/migrations/001_progress.sql`, затем `002_learning_state.sql`.
2. Включить Anonymous Sign-ins.
3. Создать `.env.local` по `.env.example`: URL и публичный anon key (не service-role).
4. Пересобрать приложение.

Существующая RLS сохранена. В облако передаются завершения и учебное состояние, черновики Blockly остаются локально. Сетевой сбой не блокирует локальное обучение. Входа в аккаунт с другого устройства пока нет. В поставляемой версии ключей нет, реальный Supabase не проверялся.

## Проверки

```powershell
npm.cmd run test:smoke
npm.cmd run build
$env:KODIK_TEST_URL='http://127.0.0.1:4173'
npm.cmd run test:ui
```

Playwright использует установленный Edge с изолированными тестовыми данными. Без переменной URL ожидает dev-сервер на 5173. Скриншоты: `artifacts/`; диагностика: `test-results/`. Результаты и ограничения — в `QA.md`, протокол настоящего повторного теста — в `USER_TESTING.md`.
